describe('ContractService', () => {
  it.todo('should execute UnseeAttachmentCommand for each unsigned contract');
  it.todo(
    'should not execute UnseeAttachmentCommand if there is no unsigned contracts',
  );
});
